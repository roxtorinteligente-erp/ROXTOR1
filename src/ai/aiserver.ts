/**
 * ROXTOR AI CORE ENGINE (DEPRECATED)
 * AI operations have been moved to the frontend as per Gemini API guidelines.
 */

export async function runAI() {
  return { error: "DEPRECATED", suggested_reply: "AI has moved to frontend." };
}
